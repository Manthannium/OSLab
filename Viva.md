# OS and kernel and shell and command interpreter

# System calls
```
fork(); wait(); getpid(); getppid();
these system calls directly interact with OS
while other function calls just plays in code only
```
